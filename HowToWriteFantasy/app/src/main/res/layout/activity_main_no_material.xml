<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".activities.MainActivity">

    <include layout="@layout/toolbar"
        android:id="@+id/tb_menu" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tb_menu"
        android:layout_alignParentBottom="true"
        android:requiresFadingEdge="vertical"
        android:fadingEdgeLength="20dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="4dp">

            <Button
                android:layout_width="match_parent"
                android:layout_height="100dp"
                android:text="Story Development"
                style="@style/AppTheme.Tile.Default"
                android:drawableStart="@drawable/ic_quill"
                android:id="@+id/story_development"
                android:background="@drawable/button"/>

            <Button
                android:layout_width="match_parent"
                android:layout_height="100dp"
                android:text="Character Development"
                style="@style/AppTheme.Tile.Default"
                android:drawableStart="@drawable/ic_person_white_48dp"
                android:id="@+id/character_development"
                android:background="@drawable/button"/>

            <Button
                android:layout_width="match_parent"
                android:layout_height="100dp"
                android:text="The World"
                style="@style/AppTheme.Tile.Default"
                android:drawableStart="@drawable/ic_terrain_white_48dp"
                android:id="@+id/the_world"
                android:background="@drawable/button"/>

            <Button
                android:layout_width="match_parent"
                android:layout_height="100dp"
                android:text="Creatures and Races"
                style="@style/AppTheme.Tile.Default"
                android:drawableStart="@drawable/ic_unicornus"
                android:id="@+id/creatures_races"
                android:background="@drawable/button"/>

            <Button
                android:layout_width="match_parent"
                android:layout_height="100dp"
                android:text="The Magic"
                style="@style/AppTheme.Tile.Default"
                android:drawableStart="@drawable/ic_magic_ball"
                android:id="@+id/the_magic"
                android:background="@drawable/button"/>

        </LinearLayout>
    </ScrollView>
</RelativeLayout>
